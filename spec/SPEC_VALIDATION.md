# Очередь задач / чеклист валидации

Это живой список: **дописывай сюда новые пункты**. После реализации и проверки отмечаем статус. Старые пункты не удаляем — так видна очередь и история.

Как отмечать:

- `[x]` сделано и проверено
- `[ ]` не сделано, ждёт очереди
- в подпункте `Проверено:` кратко чем (тест, API, UI)

Проверка от **2026-09-04**.

---

## Инфраструктура очереди

- [ ] Ко всем пользовательским сценариям должны быть тесты

  **Не сделано полностью.** API-тесты: 20 штук в `backend/tests/test_api.py`, включая оценки по критериям, баланс очередей, XLSX и студенческий сценарий. Автоматических UI/E2E-тестов пока нет; выполнены production build и ручной smoke обоих адресов.

---

## Экран авторизации

- [x] Форма входа и регистрации открывается и работает на уровне API

  Проверено: UI логина на `http://localhost:3000` (поля логин/пароль, «Войти», переход в «Регистрация» с именем, фамилией, Telegram и выбором роли). API: `POST /api/auth/login` для `reviewer`/`methodist` → 200; регистрация покрыта `test_auth_register_login_and_me`.

- [x] Несуществующий аккаунт и неверный пароль не пропускаются

  Проверено: `test_auth_rejects_unknown_user_and_wrong_password`; живой API: `ghost` / неверный пароль → 401. Без токена `GET /api/auth/me` → 401.

- [x] Пользователи сохраняются в БД и переживают перезапуск сервера

  Проверено: `test_auth_survives_app_restart` (новый `TestClient`, тот же токен и повторный логин). После рестарта uvicorn учётки `reviewer` и `methodist` на месте. Сессия в UI пишется в `localStorage` (`reviewdesk.session`) и восстанавливается через `GET /api/auth/me` при загрузке.

- [ ] Полный клик по UI: ввести пароль, войти, обновить страницу, убедиться что сессия жива

  **Не сделано автоматически** (заполнение пароля в браузере здесь не гонялось). Ручная проверка остаётся в очереди.

---

## Экран курсов

- [x] Методист может переключаться в режим ревьюера; у ревьюера переключателя нет, только бейдж «Ревьюер»

  Проверено по коду: `AppHeader` показывает `RoleToggle` только если `accountRole === "methodist"`, иначе бейдж «Ревьюер». `accountRole` берётся из роли аккаунта в сессии, а не из текущего режима просмотра. Автоматического UI-теста нет — см. пункт про тесты сценариев.

- [x] У методиста есть кнопка создания курса; ревьюер создать курс не может

  Проверено: кнопка «Создать курс» только если аккаунт методиста **и** текущий режим «Методист». В режиме ревьюера кнопка скрывается. API: `test_methodist_can_create_course_reviewer_cannot` (ревьюер 403, аноним 401, методист 201).

- [x] Старая SQLite-база не ломает создание курса (не хватало колонок `stream` и др.)

  Проверено: `ensure_schema()` добавляет недостающие колонки без удаления пользователей. Тест `test_ensure_schema_adds_missing_course_columns`. После рестарта сервера создание курса работает.

- [ ] Прогнать в браузере под методистом: переключатель ролей + создание курса; под ревьюером — нет переключателя и нет кнопки «Создать курс»

  Частично: логика кнопки исправлена (в режиме ревьюера «Создать курс» больше не показывается). Полный ручной прогон двумя учётками ещё в очереди.

---



## Домашние задания курса

- [x] У методиста на экране ДЗ курса есть кнопка «Добавить ревьюера» и выпадающий список существующих ревьюеров

  Проверено: блок «Ревьюеры» на `HomeworksScreen` в режиме методиста (select из `GET /api/reviewers` + POST на курс). В списке есть и сам методист, пометка «(вы)». API: `test_methodist_assigns_reviewer_to_course`.

- [x] Ревьюер видит только те курсы, куда его назначил методист

  Проверено: `GET /api/courses` фильтрует по `course_reviewers`. Тест: новый курс методиста ревьюеру не виден, после назначения — виден; снятие назначения скрывает курс. Демо-ревьюер назначен на активный сид «Аналитика данных».

- [x] Методист во вкладке ревьюера не видит курс, пока не назначит туда себя

  Проверено: `GET /api/courses?as_reviewer=true` для методиста без назначения → пусто; после POST себя в ревьюеры курса — курс появляется. Режим методиста по-прежнему показывает все курсы. Тест `test_methodist_reviewer_tab_requires_self_assignment`. В UI переключатель ролей запрашивает список с `as_reviewer`.

- [x] У методиста на экране ДЗ курса есть кнопка «Создать домашнее задание»

  Проверено: кнопка и форма на `HomeworksScreen` (название, дедлайн, ссылки, инструкция). API: `POST /api/courses/{id}/assignments` — `test_methodist_can_create_homework_reviewer_cannot` (401 аноним, 403 ревьюер, 201 методист, номер 3 после сида).

- [x] При создании ДЗ можно добавить критерии и их описание; ссылка на подробные критерии опциональна

  Проверено: форма создания ДЗ — список критериев (название, баллы, описание, добавить/удалить). API принимает `criteria` и пустой `criteria_url`. Тест `test_methodist_can_create_homework_reviewer_cannot` проверяет два критерия с описаниями и пустую ссылку.

- [x] Назначение ревьюера на конкретное ДЗ — выпадающий список из ревьюеров курса

  Проверено: UI «+ Назначить ревьюера» берёт `GET /api/courses/{id}/reviewers`. API: `POST /api/assignments/{id}/reviewers` с `user_id`; не зачисленный на курс → 404, повтор → 409. Тест `test_reviewer_management_and_clarification_patch`.

- [x] Если ссылка на критерии не указана, гиперссылки «Критерии» нет

  Проверено: `ResourceLinks` показывает ссылку только если `criteria_url` непустой. Раньше пустая ссылка подменялась URL задания.

- [x] Кнопка назначить ревьюера на конкретное ДЗ работает

  Причина: живой backend ещё ждал `name`/`telegram`, а UI слал `user_id` → 422, ошибка на экране не показывалась. Исправлено: API принимает `user_id`, ошибка выводится, список больше не прячет ревьюеров курса из‑за совпадения telegram с сидом. Тест `test_reviewer_management_and_clarification_patch`.

- [x] Сумма баллов критериев должна быть ровно 100

  Проверено: валидация в `AssignmentCreate` и `CriteriaUpdate`; обе формы блокируют сохранение при сумме не 100. Тест `test_criteria_must_total_exactly_100`: 99/101 → 422, 100 → 200.

- [x] При создании ДЗ можно назначить ревьюеров курса, включая «добавить всех», и убрать лишних

  Проверено: форма создания — чипы, «Добавить всех с курса», выпадающий список. `reviewer_user_ids` в POST. Тест: ДЗ создаётся сразу с demo-ревьюером курса.

---

## Новые задачи (дописывай ниже)

### Ролевые сценарии ДЗ

- [x] При редактировании критериев сумма должна быть ровно 100

  Проверено на frontend и backend: сохранение блокируется при 99/101, API принимает 100. Тест `test_criteria_must_total_exactly_100`.

- [x] При редактировании списка ревьюеров ДЗ есть кнопка «Добавить всех с курса»

  Проверено: bulk API пропускает уже назначенных и добавляет остальных. Тест `test_reviewer_only_sees_assigned_homeworks_and_bulk_add`.

- [x] Ревьюер не видит ДЗ, на которое не назначен

  Проверено: список ДЗ фильтруется по `AssignmentReviewer.user_id`, прямой переход возвращает 403. После назначения ДЗ появляется. Тест `test_reviewer_only_sees_assigned_homeworks_and_bulk_add`.

- [x] Пособие для ревьюеров видно на странице ДЗ ревьюера

  Проверено: отдельный read-only блок «Пособие по проверке» в `ReviewerHomework`.

- [x] Предложение ревьюера у методиста убирается крестиком

  Проверено: крестик переводит предложение в `dismissed`; ревьюер не может закрыть его сам. Тест `test_methodist_dismisses_reviewer_suggestion`.

### Студенческий портал

- [x] Отдельный портал доступен по другому адресу

  Проверено: `student-portal/`, Docker-сервис и UI на `http://localhost:3001`; staff UI остаётся на `http://localhost:3000`. Сессии разделены ключами `studentdesk.session` и `reviewdesk.session`.

- [x] Есть отдельная регистрация и вход студента

  Проверено: `/api/student/auth/*` всегда создаёт роль student; staff-аккаунтом войти в портал нельзя. Покрыто `test_student_portal_application_enrollment_and_submission`.

- [x] Список актуальных курсов показывает количество мест, занятость и «Мои курсы»

  Проверено: занятость считается по одобренным зачислениям, ожидающие заявки места не занимают.

- [x] Незачисленный студент видит описание курса и может откликнуться

  Проверено: заявка получает статус `pending`, повторная блокируется; методист обрабатывает её на отдельном экране «Заявки студентов».

- [x] Зачисленный студент видит баллы и ДЗ, отсортированные по дедлайну

  Проверено API-тестом: после одобрения курс появляется в «Мои курсы», задания отсортированы, сумма включает проверенные submissions.

- [x] Студент может открыть условие ДЗ и отправить работу ссылкой GitHub/Google Drive

  Проверено: допускаются только HTTPS-ссылки `github.com`, `drive.google.com`, `docs.google.com`; повторная отправка возвращает 409, критерии студенту не выдаются. Тест `test_student_portal_application_enrollment_and_submission`.

- [x] Методист принимает или отклоняет заявки на отдельном экране

  Проверено: экран «Заявки студентов» со статусами; backend повторно проверяет вместимость перед зачислением.

### Проверки этой итерации

- [x] Backend: `20 passed`
- [x] Staff frontend: ESLint и production build
- [x] Student portal: ESLint и production build
- [x] Docker Compose: 6 сервисов собраны и запущены; backend healthy; `:3000`, `:3001`, `:8000` отвечают 200
- [x] Живой Docker-сценарий: регистрация студента → заявка → одобрение методистом → ДЗ → отправка GitHub-ссылки
- [x] После рестарта Docker PostgreSQL сохраняет существующие записи (8 пользователей, 3 курса); `SEED_DEMO_DATA=false`

### Оценивание, распределение и XLSX

- [x] Ревьюер ставит оценки и комментарии по каждому критерию; итоговый балл — сумма

  Проверено: UI строк критериев с автосуммой; API валидирует набор критериев и максимумы. Тест `test_main_reviewer_flow` (перебор максимума → 422, сумма 39 сохраняется, PDF отдаётся).

- [x] Студент не видит критерии ни в UI, ни в API

  Проверено: `StudentAssignmentOut` без `criteria`/`criteria_url`; портал показывает только условие и статус. Тест проверяет отсутствие этих полей.

- [x] Студент отправляет работу только один раз

  Проверено: второй POST → 409; после отправки форма в портале заменяется read-only статусом.

- [x] Непроверенные работы перераспределяются равномерно при добавлении/удалении ревьюеров ДЗ

  Проверено: 4 работы на одном ревьюере → после второго 2/2; после удаления снова 4. Новая отправка идёт к ревьюеру с минимальной нагрузкой. Тест `test_rebalance_and_xlsx_roundtrip`.

- [x] Ревьюер видит свою очередь и историю проверок; `next` отдаёт только его работы

  Проверено: список submissions фильтруется по `reviewer_user_id`; GET `/api/submissions/{id}` доступен владельцу и методисту, чужому ревьюеру → 403. Тест `test_reviewer_history_and_submission_modal`.

- [x] Методист и ревьюер открывают проверенную работу в overlay

  Проверено по коду: общий `Modal` в `ReviewerHomework` (редактирование) и `MethodistHomework` (просмотр). Автоматического UI-теста нет.

- [x] Ревьюер и методист редактируют описание курса; заявки фильтруются по `course_id`

  Проверено: `PATCH /api/courses/{id}` — методист всегда, ревьюер только если назначен на курс. Вкладка «Заявки студентов» на странице курса. Тест `test_course_description_acl_and_applications_filter`.

- [x] Автосид выключен по умолчанию, текущая БД не чистится

  Проверено: `SEED_DEMO_DATA` по умолчанию false; тест `test_lifespan_does_not_seed_by_default`. После Docker rebuild счётчики users/courses не обнулились.

- [x] Методист выгружает XLSX курса и ДЗ и импортирует ревьюеров по колонке `login`

  Проверено: курс — листы Students/Reviewers/Applications; ДЗ — Submissions/Reviewers/Scores. Неизвестный логин не создаёт аккаунт и попадает в `errors`; preview не назначает, confirm назначает и перераспределяет. Живой Docker: `GET /api/courses/{id}/export.xlsx` → 200 Excel. Тест `test_rebalance_and_xlsx_roundtrip`.

- [ ] Полный клик по UI: критерии, modal, вкладка заявок курса, XLSX download/upload

  **Не сделано автоматически.** API и production build проверены; ручной E2E в браузере остаётся в очереди.
